"""A package of handlers, loaded by examples/plugin_bot.py.

An __init__.py is what makes this a package rather than a directory, which is
what makes each plugin a real module with a real name, which is what makes a
traceback from inside one say where it came from.
"""
